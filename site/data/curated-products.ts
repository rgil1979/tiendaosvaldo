// Fallback de productos curados — ya no se usa activamente.
// El API route ahora usa getProducts() en tiempo real desde ML.
// Mantenido como referencia; no importar en código nuevo.

export const curatedProducts: Record<string, unknown[]> = {
  default: [],
}
